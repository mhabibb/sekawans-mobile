import 'package:flutter/material.dart';

class DeleteView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Hapus Data'),
      ),
      body: const Center(
        child: Text('Halaman Hapus Data'),
      ),
    );
  }
}
