import 'package:flutter/material.dart';

class PrintView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cetak/Unduh Data'),
      ),
      body: const Center(
        child: Text('Halaman Cetak/Unduh Data'),
      ),
    );
  }
}
