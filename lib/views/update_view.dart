import 'package:flutter/material.dart';

class UpdateView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Update Data'),
      ),
      body: const Center(
        child: Text('Halaman Update Data'),
      ),
    );
  }
}
